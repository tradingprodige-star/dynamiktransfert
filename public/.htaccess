Options -MultiViews
RewriteEngine On
RewriteBase /

# Serve existing files and directories normally.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Let the React app handle public and admin routes.
RewriteRule ^ index.html [L]
